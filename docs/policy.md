# Policy and cache

Taxonomy and risk statements are replaceable operator data. A semantic score never directly enforces: excluded categories and dimension ceilings produce allow, review or block in code. Content, taxonomy version and policy version form the cache identity; shadow mode preserves comparison evidence without enforcement.
